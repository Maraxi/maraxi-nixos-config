#! /usr/bin/env bash

set -euo pipefail

manpage=$(
    IFS=":"
    fd --print0 --follow --format '{/.}' '.gz$' $(manpath) \
	| sort --zero-terminated --unique \
	| fzf --read0 --highlight-line --preview 'man {}'
)

man "$manpage"
